#!/usr/bin/env bash

# NOTE : cannot use init script system for service because some of them need docker to stop/wtart service toi pregenerate sone config files
#        see __sabnzbd_init and __sabnzbd_first_launch
#        at best some settings could be done from a container with an init script here
#        see __sabnzbd_settings
#           
